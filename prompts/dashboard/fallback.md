Build a clean, mobile-first analytics dashboard tab for this dataset. Infer the data's shape from the column names and values and the data-analysis summary; do not assume a fixed schema.

Load the real rows at runtime from `useProtectedDashboardData('<dashboard-name>')` — there is no file upload. Derive everything from those rows, and guard against missing, null, or unparseable fields so the component never throws.

Choose a compact set of KPIs and 1-3 Recharts views that best fit the detected measures, dimensions, date span, and sample size; these are defaults, not a quota. Use a trend with the shared Weekly / Monthly / Quarterly `.segmented` control only when at least two meaningful periods exist. Apply the evidence-first Top Insights contract from the system prompt: render only the strongest 2-5 measured findings, fewer when evidence is weak, with context and no invented causes, savings, alternatives, or forecasts. Usually finish with a searchable recent-records `.data-table` when row inspection is useful. Give every chart an explicit margin, truncate long horizontal-bar labels with a fixed YAxis width, and skip degenerate or mostly-missing breakdowns. Use the OpenBoardCLI classes and CSS variables with `var(--chart-N)` colors, format values via Intl/date-fns, keep charts labelled, add no dependencies, and never embed raw rows in source.
