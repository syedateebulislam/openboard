Build a clean, mobile-first analytics dashboard tab for this dataset. Infer the data's shape from the column names and values and the data-analysis summary; do not assume a fixed schema.

Load the real rows at runtime from `useProtectedDashboardData('<dashboard-name>')` — there is no file upload. Derive everything from those rows, and guard against missing, null, or unparseable fields so the component never throws.

Include: a row of KPI summary cards for the key aggregate metrics (with a delta vs the previous period when a date exists); 2-3 Recharts charts that fit the data (a trend over time with a Weekly / Monthly / Quarterly period toggle using the shared `.segmented` control when a date exists, a category breakdown, and a top-N ranking); and a "Top Insights" section of exactly 4 InsightCard tiles — 2 cost/risk-flavored (tone "spend") followed by 2 opportunity/improvement-flavored (tone "save") plain-language observations. End the tab with a REQUIRED "Recent Records" `.data-table` of the rows sorted newest-first (10 most recent visible by default, paginated). Give every chart an explicit Recharts margin, truncate long category labels (fixed YAxis width on horizontal bars), and skip pie charts for dimensions with fewer than 2 meaningful values. Use the OpenBoardCLI design-system utility classes and CSS variables with `var(--chart-N)` series colors (never hardcoded colors), format numbers and dates via Intl and date-fns, and keep every chart labelled. Do not add new dependencies and do not embed raw rows in source.
